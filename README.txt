Sprint One
- testing is done
	a) within the module itself
	b) inside /modules, prefix test_*.py
	c) inside view/cool/, there are test.json objects

for integrating components, we checked that the output is equivalent to what we expected.
